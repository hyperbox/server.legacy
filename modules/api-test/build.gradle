apply plugin: 'java'
apply plugin: 'maven'

group = 'io.kamax'
version = 'latest'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile project(':hbox-server-api')
    compile 'junit:junit:4.12'
}
